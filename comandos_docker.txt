### 🚀 Comandos Docker Essenciais

# 1. Adicionar usuário ao grupo docker (para não precisar de sudo)
sudo usermod -aG docker $USER
newgrp docker

# 2. Listar imagens disponíveis
docker images

# 3. Listar containers ativos
docker ps

# 4. Listar todos os containers (incluindo parados)
docker ps -a

# 5. Rodar uma imagem
docker run nome_imagem

# 6. Rodar em segundo plano (detached)
docker run -d nome_imagem

# 7. Rodar com mapeamento de porta (host:container)
docker run -d -p 5000:5000 flask-aws

# 8. Rodar com porta diferente no host (8080 -> 5000 do container)
docker run -d -p 8080:5000 flask-aws

# 9. Parar container
docker stop <id_container>

# 10. Remover container
docker rm <id_container>

# 11. Remover imagem
docker rmi <nome_imagem>

# 12. Ver logs de um container
docker logs <id_container>

# 13. Acessar terminal dentro de um container
docker exec -it <id_container> bash

# 14. Buildar imagem a partir de Dockerfile
docker build -t flask-aws .